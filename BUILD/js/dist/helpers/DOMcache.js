/////////////////////////////////
// Global cached DOM elements

export default {
    $HTML : $(document.documentElement),
    $DOC  : $(document),
    $WIN  : $(window),
    $BODY : $(document.body)
}