/////////////////////////////////
// Global cached DOM elements

var DOM = {
    $HTML : $(document.documentElement),
    $DOC  : $(document),
    $WIN  : $(window),
    $BODY : $(document.body)
}

export default DOM;